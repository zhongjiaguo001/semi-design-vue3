import Progress, { progressProps } from './Progress';

export { Progress, progressProps };
export type { ProgressType, ProgressSize, ProgressDirection, ProgressStrokeLinecap, StrokeItem, StrokeArr } from './Progress';
export default Progress;
