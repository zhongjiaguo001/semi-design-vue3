import MarkdownRender, { markdownRenderProps, markdownRenderEmits } from './MarkdownRender';
import { defaultMarkdownComponents, h1, h2, h3, h4, h5, h6, a, img, table, p, code } from './components';

export {
  MarkdownRender,
  markdownRenderProps,
  markdownRenderEmits,
  defaultMarkdownComponents,
  h1,
  h2,
  h3,
  h4,
  h5,
  h6,
  a,
  img,
  table,
  p,
  code,
};
export default MarkdownRender;
