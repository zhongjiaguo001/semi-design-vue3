import AIChatInput, { aiChatInputProps, aiChatInputEmits } from './AIChatInput';
import Configure, { getConfigureItem, ConfigureButton, ConfigureSelect, ConfigureMcp, ConfigureRadioButton } from './configure';
import { getCustomSlotAttribute } from '@douyinfe/semi-foundation/lib/es/aiChatInput/utils';

export {
  AIChatInput,
  aiChatInputProps,
  aiChatInputEmits,
  Configure,
  getConfigureItem,
  ConfigureButton,
  ConfigureSelect,
  ConfigureMcp,
  ConfigureRadioButton,
  getCustomSlotAttribute,
};
export default AIChatInput;
