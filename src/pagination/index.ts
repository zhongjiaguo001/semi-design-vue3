import Pagination, { paginationProps, paginationEmits } from './Pagination';

export { Pagination, paginationProps, paginationEmits };
export type { PaginationSize, PageRenderText } from './Pagination';
export default Pagination;
