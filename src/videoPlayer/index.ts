import VideoPlayer, { videoPlayerProps, videoPlayerEmits } from './VideoPlayer';
import VideoProgress, { videoProgressProps, videoProgressEmits } from './videoProgress';
import { formatTime } from './utils';

export { VideoPlayer, VideoProgress, videoPlayerProps, videoPlayerEmits, videoProgressProps, videoProgressEmits, formatTime };
export default VideoPlayer;
